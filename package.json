{
  "name": "opc-automation-demo",
  "version": "1.0.0",
  "description": "Demo application showing OPC automation error handling and solutions for HRESULT E_FAIL issues",
  "main": "index.js",
  "scripts": {
    "dev": "node server/server.js",
    "build": "npm run build:client",
    "build:client": "cd client && npm run build",
    "install:all": "npm install && cd client && npm install",
    "test": "node tests/test-opc-connection.js"
  },
  "keywords": [
    "opc",
    "automation",
    "hresult",
    "error-handling",
    "demo"
  ],
  "author": "OPC Demo",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "ping": "^0.4.4"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
